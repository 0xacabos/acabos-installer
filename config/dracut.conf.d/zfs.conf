# dracut configuration for ZFS boot.
# Installed into chroot /etc/dracut.conf.d/zfs.conf during BOOT_CHAIN.
# ZFS modules included (boot-critical), NVIDIA excluded (early-graphics, loaded later).
add_dracutmodules+=" zfs "
omit_dracutmodules+=" nvidia "
install_items+=" /etc/zfs/zpool.cache "
